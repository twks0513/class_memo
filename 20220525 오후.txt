[함수]

round(컬럼명,x) from (테이블명) - 컬럼명에 있는 값을 소수점 x자리까지 반올림한다

trunc(컬럼명,x) from (테이블명) - 컬럼명에 있는 값을 소수점 x자리까지 버림한다

mod(컬럼명,x) from (테이블명) - 나머지 값을 보여준다

upper(컬럼명) from (테이블명) - 컬럼에 있는 모든 문자를 대문자로 변환

lower(컬럼명) from (테이블명) - 컬럼에 있는 모든 문자를 소문자로 변환

initcap(컬럼명) from (테이블명)- 컬럼에 있는 모든 문자의 첫번째 문자를 대문자로 변환

sum(컬럼명) from (테이블명) - 컬럼에 있는 모든 값을 더해준다
avg(컬럼명) from (테이블명) - 컬럼에 있는 모든 값의 평균을 구해준다
max(컬럼명) from (테이블명) - 컬럼에 있는 가장 큰 값을 출력한다
min(컬럼명) from (테이블명) - 컬럼에 있는 가장 작은 값을 출력한다
count(컬럼명) from (테이블명) - 컬럼에 있는 값들의 갯수를 세준다(컬럼명에 *을 넣을경우 null값도 세준다)

group by (컬럼명); - 컬럼에 맞춰서 그룹으로 만들어 준다
